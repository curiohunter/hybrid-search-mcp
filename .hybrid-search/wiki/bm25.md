# Bm25

**Files**: 1 | **Symbols**: 5

## Files

- `src/hybrid_search/search/bm25.py`

## Entry Points

- `src/hybrid_search/search/bm25.py::BM25Engine`

## Symbols

### `src/hybrid_search/search/bm25.py`

- **BM25Result** (dataclass, L17)
  - Fields: chunk_id, score
- **BM25Engine** (class, L23)
  - Tantivy BM25 full-text search index with read_only mode support
  - Schema fields: chunk_id (stored, raw tokenizer), name, qualified_name, content, docstring
  - Schema mismatch: auto-recreates index in write mode
  - calls: add, commit, delete, delete_batch, search
- **add** (function, L71)
  - Deduplication: calls `writer.delete_documents("chunk_id", chunk_id)` before adding
  - Prevents duplicate BM25 entries on re-index without explicit delete
- **search** (function, L109)
  - BM25 scoring across all text fields (name, qualified_name, content, docstring)
  - Query escaping fallback for Tantivy special characters
  - called by: BM25Engine, _search_cross_project, _search_single
- **count** (property, L142)
  - Approximate document count via searcher.num_docs
